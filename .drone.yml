pipeline:
  test:
    image: python:3.6
    environment:
      - POSTGRES_HOST=postgres
      - POSTGRES_USER=url
      - POSTGRES_PASSWORD=secret
    commands:
      - pip install pipenv
      - cd app
      - pipenv install --system
      - python app.py &
      - sleep 5
      - "curl -s localhost:8000/health | grep 'true'"
    when:
      branch: [master, cicd]

  publish:
    image: plugins/docker:17.10
    repo: cuongnv23/url-shortener-app
    dockerfile: app/Dockerfile
    context: app
    secrets: [ docker_username, docker_password ]
    tag: latest
    when:
      branch: [ master ]

  telegram:
    image: appleboy/drone-telegram
    debug: true
    to: cuongnv23
    token: 595280182:AAGlO4YA-1IFuSLrT2yA2IC_2Zuz-lan2Ms
    format: markdown
    message: >
      build {{build.number}} {{build.status}}.
    when:
      status: [ success, failure ]

services:
  postgres:
    image: cuongnv23/url-shortener-db
